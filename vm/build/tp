#!/bin/sh

make all
#pintos -v -k -T 60 --qemu  --fs-disk=2 \
#       -p tests/vm/$1 \
#       -a $1 \
#       --swap-disk=9 \
#       -- -q -f run $1;

pintos -v -k -T 600 --qemu  --fs-disk=2 -p tests/vm/mmap-shuffle -a mmap-shuffle --swap-disk=4 \
    -- -q   -f run mmap-shuffle
